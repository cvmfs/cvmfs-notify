[
 {lager, [{log_root, "/tmp"},
          {crash_log, "cvmfs-notify-crash.log"},
          {handlers, [{lager_console_backend, [{level, debug}]}]},
          {extra_sinks, [{error_logger_lager_event, [{handlers, [{lager_console_backend, [{level, info}]}]}]}]}]},

 {cvmfs_notify, [{user_config, {file, "./config/config.json"}}]}
].
